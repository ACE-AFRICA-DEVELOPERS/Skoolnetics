include includes/header.pug
block content
    link(rel='stylesheet' , href='/stylesheets/dashboard.css')
    body
        .loader-bg
            .loader-track
                .loader-fill
        include includes/sidenav.pug 
        include includes/usernav.pug
        .pcoded-main-container
            .pcoded-wrapper
                .pcoded-content
                    .pcoded-inner-content
                        .main-body
                            .page-wrapper
                                .row
                                    .col-md-6
                                        if success
                                            if success.length != 0
                                                .alert.alert-success.alert-dismissible.fade-in(role="alert")
                                                    button.close(type="button" data-dismiss="alert" aria-label="close")
                                                        span(aria-hidden="true") ×
                                                    span #{success}
                                .card
                                    .card-header
                                        h5 Roles
                                        span.d-block.m-t-5 Select one or more actions to create a role:
                                    .card-body
                                        form(method="POST")
                                            .row
                                                .col-md-6
                                                    label Role Name:
                                                    input(type="text" class="form-control mb-3" name="role")
                                                    p Actions:
                                                    input(type="checkbox" name="finance")
                                                    label(class="label") Manage Finances
                                                    input(type="checkbox" name="finance")
                                                    label(class="label") Approve Lesson Notes
    
        include includes/footer.pug